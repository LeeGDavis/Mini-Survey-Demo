# Initialize the router
define ["router/router"], (Router) ->
  initialize = ->
    
    # Pass in our Router module and call it's initialize function
    Router.initialize()

  initialize: initialize

